Release Evidence Room uses four WebMCP tools to turn a green dashboard into an auditable human decision. Here, eighteen of eighteen tests pass. The prompt asks the agent to review payment-retry safety using only evidence owned by this page.

First, get release snapshot returns state version twelve and the missing coverage. Query network evidence filters the bounded synthetic records. One payment intent has two accepted operation references and different idempotency key references. This is a release risk, not proof of a duplicate charge.

Propose test case creates a pending response-loss regression test. WebMCP does not approve or execute it. I approve it as the human release owner.

The agent reads state fourteen, then propose release decision recommends HOLD. The header remains undecided until I click Confirm HOLD.

All four tools use strict schemas and runtime validation. Repeated request IDs are idempotent; stale versions fail without mutation. Every read, proposal, and human action is attributed and versioned. There are no credentials, live company systems, autonomous approvals, or deployment tool. The agent builds the evidence case. The human makes the release decision.
