Eighteen out of eighteen release checks passed. Would you ship this payment retry build?

Release Evidence Room helps QA leads and release owners turn evidence missed by green dashboards into auditable release decisions.

First, this fictional checkout accepts a payment attempt but loses the response. Retrying the same intent with a new idempotency key creates a second accepted operation. No account, card, backend, or real payment is involved. I send that browser-local session into the Release Evidence Room.

Inside ChatGPT's in-app browser, the page exposes five WebMCP tools to read the release snapshot, query redacted evidence, propose a test, run approved verification, and propose a release decision. The agent links both attempts to a missing response-loss test. It does not scrape the interface or claim that a customer was charged twice.

The test proposal stays pending. When the agent tries to run it early, the page rejects the request. I approve the test as the human release owner.

Only then can verification execute. The targeted replay starts from a fresh checkout-model session and reproduces two modeled side effects. A seeded monkey run, capped at twenty steps, reaches the same failure through a deterministic sequence. Both results say risk confirmed for this synthetic model, not for a live payment service.

The safeguards are visible too. Retrying an identical request ID replays the prior result without duplication. A stale state version is rejected instead of overwriting newer evidence.

The agent cannot turn that risk into READY, so the page rejects the attempt. It proposes HOLD, but the release header remains undecided until I confirm it.

Every read, proposal, verification, and human action is attributed and versioned. No credentials or company data are exposed. Those five page tools expose no arbitrary traffic, code, approvals, or deployments. It assembles and verifies the bounded evidence case. The human makes the release decision.
